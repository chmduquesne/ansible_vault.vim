name: Vim Tests

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Vim
        run: sudo apt-get install -y vim ansible

      - name: Run tests
        run: ./test/run.sh
